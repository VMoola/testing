# Script configs

This is a collection of all configs related to the example scripts. We can copy
this directory as is to a new top level ```configs``` directory for a default example set.

The naming scheme for the configs is [script_name].conf. This is so that we can have all
the bash scripts call a [generic script](/examples/example_scripts/host/load_config) to
load the relevant config.
